import draggable from './module/draggable'
import clipboard from './module/clipboard'
import hasPerms from './module/hasPerms'

const directives = {
  draggable,
  clipboard,
  hasPerms
}

export default directives
